# Firmware Build Map

Auto-generated by `firmware/rp2040/scripts/inject_build_id.py`.
Each row records one PlatformIO build with a unique, auto-incremented `FW_BUILD_ID`.

| build_id | git_commit | binary_name   | date                |
|----------|------------|---------------|---------------------|
