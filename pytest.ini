[pytest]
markers =
    hardware: marks tests that require physical hardware (TX board + RP2040 + LR2021)
    simulate: marks tests that run in simulation (no hardware needed)
testpaths = tests/src
addopts = -v --tb=short
